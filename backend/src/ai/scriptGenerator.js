export async function generateScript(brand, tone, audience) {
  // TODO: Replace with real Transformers.js model
  return `
    Introducing ${brand}.
    A ${tone} message crafted for ${audience}.
    This is a placeholder AI script.
  `;
}
